本文件是p10的高级封装

所有的用例都用了夹具，这就属于夹具的二次封装

自动化用例写着写着一边写一遍封装，比如说定位元素、配置文件、日志文件、数据驱动这些东西我们慢慢的都会封装到一起，封装到一起我们管这个叫方法层


自动化框架架构
一、
POM层：POM层又分为了3层
    第一层、BasePage基础页面层
    第二层、PageObject页面对象层（把所有的页面定位元素、元素的对象，元素的动作都放在这一层）
    第三层、测试用例层（测试用例是完全独立的，在里面没有其它的任何东西，只有用例）
二、
封装的方法层：
    1自定义模块
    2所调用的第三方库
    3数据驱动方法DDT
    4全局inl配置文件
    5yaml配置文件
    6日志操作方法
三、
excel/csv/yaml/mysql数据库（就是说数据是从哪里来的）
四、
HTML报告、日志文件、其他图片附件、邮件文件(主要关于输出的）

以上便是组成自动化框架的四大块，运行需要用Jenkins做底层的持续集成，还有版本控制工具GIT、GltHub、码云跟我们的开发工具pycharm去持续集成












